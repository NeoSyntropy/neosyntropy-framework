"""Unified database layer for NeoSyntropy.

Subpackages:
- vector/    — vector store adapters (Chroma, Qdrant, Weaviate, Redis, ...)
- graph/     — graph databases (Neo4j)
- relational/ — SQL databases (Postgres, DuckDB)
- document/  — document stores (MongoDB)
- storage/   — object / blob stores (S3, GCS, Azure Blob)
"""
