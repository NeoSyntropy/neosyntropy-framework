# Optional graph governance publication + run observers.
# Enabled only by the literal uppercase string TRUE (true / 1 / yes stay off).
# See docs/remote-execution.md
NEOSYNTROPY_MONITOR=

# Optional executable graph upload, backend-owned control, and FSM.load.
# Literal uppercase TRUE also enables graph governance monitoring.
NEO_REMOTE_EXECUTION=
